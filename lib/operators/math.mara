module collection

import 'mara.collection' [
    Collection:
]

use Collection

alias('++', concat)
alias('--', diff)
alias('**', dotproduct)

alias('//', intdiv)

end


module

alias('+', plus  )
alias('-', minus )
alias('*', times )
alias('/', divide)
alias('^', power )

end
